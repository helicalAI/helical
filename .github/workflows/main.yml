name: CI Pipeline

on:
  pull_request:
    branches:
      - main

jobs:
  build:
  
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    timeout-minutes: 60 
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: setup python
        uses: actions/setup-python@v5
        with:
          python-version: 3.11.8
          cache: 'pip' # caching pip dependencies

      - name: Install dependencies
        run: |
            pip install -r requirements-dev.txt
            pip install .
            
      # First download before tests as they make use of the downloaded files 
      - name: Download all files
        run: |
          python ci/download_all.py

      - name: Execute unittests
        run: |
          pytest --cov-report "xml:coverage.xml" --cov-branch --cov=helical ci/ --cov-report=term
               
      - name: Pytest coverage comment
        uses: MishaKav/pytest-coverage-comment@main
        with:
          pytest-xml-coverage-path: ./coverage.xml

      # - name: Execute scGPT
      #   run: |
      #     python examples/run_scgpt.py
      # - name: Execute Geneformer
      #   run: |
      #     python examples/run_geneformer.py
      # - name: Execute UCE
      #   run: |
      #     python examples/run_uce.py
      # - name: Execute Hyena
      #   run: |
      #     python examples/run_hyena_dna.py